package class8;

public class E3ForLoop {
    public static void main(String[] args) {
        int sum=0;// creating a variable to hold the sum
        for (int a=1;a<=10;a++){
            if (a%2==0){// checking if a  number is even
                sum=sum+a;// sum+=a;// adding all the even numbers to the sum variable one by one

            }


            }
        System.out.println(sum);
        }

    }

